build:
  docker:
    web: Dockerfile
release:
  command:
   - python manage.py makemigrations && python manage.py migrate
run:
  web: gunicorn app.wsgi:application --bind 0.0.0.0:$PORT
  worker: celery -A app beat -l INFO